# EXAMPLE ONLY. Run python scripts/init_local.py to generate the secret and first admin.
# Never use fixed placeholder credentials or expose this prototype publicly.
TRUST_ENV=local
TRUST_DB=data/trust.db
# TRUST_HMAC_KEY is generated into .env by scripts/init_local.py.
# Staff sign-in uses named accounts with a TOTP second factor, held in the database,
# not shared keys in this file. Manage accounts with scripts/create_staff.py.
